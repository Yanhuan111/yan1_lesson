<view class="container legend-list">
    <block wx:for="{{legends}}">

        <view class="image-section">
            <!-- 显示英雄头像 -->
        <image style="width:80px;height:80px;background-color:#eeeeee" mode="aspectFit" src="{{item.src}}"/>
        </view>
        <text >{{item.name}} {{item.title}}</text>
    </block>
</view>
